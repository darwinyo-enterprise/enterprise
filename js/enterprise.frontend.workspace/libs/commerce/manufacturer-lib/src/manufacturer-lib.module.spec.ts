import { ManufacturerLibModule } from './manufacturer-lib.module';

describe('ManufacturerLibModule', () => {
  it('should work', () => {
    expect(new ManufacturerLibModule()).toBeDefined();
  });
});
