apiVersion: v1
kind: Service
metadata:
  name: {{ template "enterprise.registry.fullname" . }}
  labels:
    app: {{ template "enterprise.registry.name" . }}
    chart: {{ template "enterprise.registry.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "enterprise.registry.name" . }}
    release: {{ .Release.Name }}
