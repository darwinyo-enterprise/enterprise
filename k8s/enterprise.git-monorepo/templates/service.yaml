apiVersion: v1
kind: Service
metadata:
  name: {{ template "enterprise.git-monorepo.fullname" . }}
  labels:
    app: {{ template "enterprise.git-monorepo.name" . }}
    chart: {{ template "enterprise.git-monorepo.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "enterprise.git-monorepo.name" . }}
    release: {{ .Release.Name }}
