apiVersion: v1
kind: Service
metadata:
  name: {{ template "enterprise.jenkins.fullname" . }}
  labels:
    app: {{ template "enterprise.jenkins.name" . }}
    chart: {{ template "enterprise.jenkins.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "enterprise.jenkins.name" . }}
    release: {{ .Release.Name }}
