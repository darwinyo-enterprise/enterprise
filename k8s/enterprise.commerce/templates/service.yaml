apiVersion: v1
kind: Service
metadata:
  name: {{ template "enterprise.commerce.fullname" . }}
  labels:
    app: {{ template "enterprise.commerce.name" . }}
    chart: {{ template "enterprise.commerce.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "enterprise.commerce.name" . }}
    release: {{ .Release.Name }}
